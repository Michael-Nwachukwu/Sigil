{
  "name": "sigil-demo",
  "version": "0.0.1",
  "private": true,
  "description": "Sigil Protocol — demo agents + scenarios that produce real on-chain provenance records on 0G Galileo testnet.",
  "license": "MIT",
  "scripts": {
    "risk-scorer": "tsx scripts/risk-scorer.ts",
    "audit-agent": "tsx scripts/audit-agent.ts",
    "prompt": "tsx scripts/prompt.ts",
    "notarize-output": "tsx scripts/notarize-output.ts",
    "scenario1": "tsx scenarios/scenario1-identity-resolution.ts",
    "scenario2": "tsx scenarios/scenario2-provenance-notarization.ts",
    "scenario3": "tsx scenarios/scenario3-living-resume.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .fixtures",
    "lint": "echo 'no linter configured for demo package'",
    "build": "echo 'demo package is run-only (tsx); no build step'",
    "test": "echo 'no tests for demo package'"
  },
  "dependencies": {
    "sigil-protocol": "workspace:*",
    "ethers": "^6.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "dotenv": "^16.6.1",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
